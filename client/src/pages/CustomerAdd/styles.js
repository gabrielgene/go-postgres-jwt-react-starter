import styled from 'styled-components';
import MUITextField from '@material-ui/core/TextField';

export const TextField = styled(MUITextField).attrs({ fullWidth: true })`
  margin-bottom: 16px;
  margin-right: 24px;
`;

export const DialogWrapper = styled.div``;
